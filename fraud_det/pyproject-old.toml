[project]
name = "fraud_det"
version = "0.1.0"
description = "fraud_det using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]==1.6.0"
]

[project.scripts]
fraud_det = "fraud_det.main:run"
run_crew = "fraud_det.main:run"
train = "fraud_det.main:train"
replay = "fraud_det.main:replay"
test = "fraud_det.main:test"
run_with_trigger = "fraud_det.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
